{
  "name": "sniper-range",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DEAD EYE — a desert long-range bottle-shooting game rendered on canvas.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "~5.4.10"
  }
}
